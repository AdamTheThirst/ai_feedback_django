"""Конфигурация приложения ``core``."""

from django.apps import AppConfig


class CoreConfig(AppConfig):
    """Определяет базовые параметры загрузки технического приложения ``core``.

    Контекст использования:
        Подключается в ``INSTALLED_APPS`` для инициализации общих URL и шаблонов.

    Параметры:
        Использует стандартные параметры ``AppConfig`` Django.

    Возвращаемое значение:
        Экземпляр конфигурации приложения, создаваемый Django автоматически.

    Исключения и особые случаи:
        Специальная обработка исключений не предусмотрена.

    Побочные эффекты:
        Сообщает Django имя и тип автоинкрементного поля по умолчанию.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.core"
    verbose_name = "Ядро проекта"
