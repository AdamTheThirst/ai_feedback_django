[project]
name = "ai-feedback-django"
version = "0.1.0"
description = "Django-проект с разделом энциклопедии"
requires-python = ">=3.11"
dependencies = [
  "Django>=5.1,<6.0"
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
python_files = ["tests.py", "test_*.py", "*_tests.py"]
